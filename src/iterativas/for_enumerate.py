# Lista de nombres
nombres = ["Ana", "Carlos", "Elena"]

# enumerate devuelve pares (indice, valor)
for indice, nombre in enumerate(nombres):
    # Mostramos índice y valor directamente
    print(f"Posición {indice}: {nombre}")