# Lista de nombres
nombres = ["Ana", "Carlos", "Elena"]

# len(nombres) devuelve la cantidad de elementos
# range(len(nombres)) genera índices: 0, 1, 2
for i in range(len(nombres)):
    # Accedemos al elemento usando su índice
    print(f"Posición {i}: {nombres[i]}")